// hooks
import { useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";

// components

// layout
import MainPageLayout from "../../layouts/mainPage";

const Vendor = () => {
  return <MainPageLayout>Vendor</MainPageLayout>;
};

export default Vendor;
